@model IEnumerable<tfzr_rsok_autosalon.Viewmodels.ManufacturersViewModel>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Manufacturers</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @item.Model.Name
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Model.Id }, new {@class="btn-warning btn"})
                @Html.ActionLink("Delete", "Delete", new { id = item.Model.Id }, new { @class = "btn-danger btn" })
            </td>
        </tr>
    }
    </tbody>
</table>